# Copy to .env (gitignored) and fill in, or just `export ANTHROPIC_API_KEY=...`
# in your shell. Never commit real keys.
ANTHROPIC_API_KEY=

# Needed on the RunPod pod only, to download Llama 3.1 weights:
HUGGING_FACE_HUB_TOKEN=
