# Runs on the RunPod GPU pod: QLoRA fine-tuning and LoRA merge (the merged
# model is then served directly via vLLM, in a separate venv — see
# catalog/structured_client.py docstring for the vllm serve command).
torch>=2.3.0
transformers>=4.44.0
peft>=0.12.0
trl>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.33.0
datasets>=2.20.0
sentencepiece>=0.2.0
protobuf>=4.25.0
